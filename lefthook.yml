pre-commit:
  commands:
    build:
      run: |
        echo "Building website..."
          yarn build
          if [ $? -ne 0 ]; then
            echo "Build failed"
            exit 1
          fi
