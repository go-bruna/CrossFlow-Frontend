// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/chain/balance_observation.proto

/* eslint-disable */
// @ts-nocheck
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.chain";

/** BalanceObservation defines the balance update observation. */
export interface BalanceObservation {
  id: number;
  chainId: string;
  balance: string;
  decimal: number;
  tokenSymbol: string;
  isNative: boolean;
  used: boolean;
  voter: string;
}

function createBaseBalanceObservation(): BalanceObservation {
  return { id: 0, chainId: "", balance: "", decimal: 0, tokenSymbol: "", isNative: false, used: false, voter: "" };
}

export const BalanceObservation: MessageFns<BalanceObservation> = {
  encode(message: BalanceObservation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.chainId !== "") {
      writer.uint32(18).string(message.chainId);
    }
    if (message.balance !== "") {
      writer.uint32(26).string(message.balance);
    }
    if (message.decimal !== 0) {
      writer.uint32(32).uint64(message.decimal);
    }
    if (message.tokenSymbol !== "") {
      writer.uint32(42).string(message.tokenSymbol);
    }
    if (message.isNative !== false) {
      writer.uint32(48).bool(message.isNative);
    }
    if (message.used !== false) {
      writer.uint32(56).bool(message.used);
    }
    if (message.voter !== "") {
      writer.uint32(66).string(message.voter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BalanceObservation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBalanceObservation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.chainId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.balance = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.decimal = longToNumber(reader.uint64());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.tokenSymbol = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.isNative = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.used = reader.bool();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.voter = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BalanceObservation {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      chainId: isSet(object.chainId) ? globalThis.String(object.chainId) : "",
      balance: isSet(object.balance) ? globalThis.String(object.balance) : "",
      decimal: isSet(object.decimal) ? globalThis.Number(object.decimal) : 0,
      tokenSymbol: isSet(object.tokenSymbol) ? globalThis.String(object.tokenSymbol) : "",
      isNative: isSet(object.isNative) ? globalThis.Boolean(object.isNative) : false,
      used: isSet(object.used) ? globalThis.Boolean(object.used) : false,
      voter: isSet(object.voter) ? globalThis.String(object.voter) : "",
    };
  },

  toJSON(message: BalanceObservation): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.chainId !== "") {
      obj.chainId = message.chainId;
    }
    if (message.balance !== "") {
      obj.balance = message.balance;
    }
    if (message.decimal !== 0) {
      obj.decimal = Math.round(message.decimal);
    }
    if (message.tokenSymbol !== "") {
      obj.tokenSymbol = message.tokenSymbol;
    }
    if (message.isNative !== false) {
      obj.isNative = message.isNative;
    }
    if (message.used !== false) {
      obj.used = message.used;
    }
    if (message.voter !== "") {
      obj.voter = message.voter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BalanceObservation>, I>>(base?: I): BalanceObservation {
    return BalanceObservation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BalanceObservation>, I>>(object: I): BalanceObservation {
    const message = createBaseBalanceObservation();
    message.id = object.id ?? 0;
    message.chainId = object.chainId ?? "";
    message.balance = object.balance ?? "";
    message.decimal = object.decimal ?? 0;
    message.tokenSymbol = object.tokenSymbol ?? "";
    message.isNative = object.isNative ?? false;
    message.used = object.used ?? false;
    message.voter = object.voter ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
