// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/lock/genesis.proto

/* eslint-disable */
// @ts-nocheck
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AssetLockTransaction } from "./asset_lock_transaction";
import { LockBalance } from "./lock_balance";
import { ObserveVoteLocking } from "./observe_vote_locking";
import { Params } from "./params";
import { SupplyHashObservation } from "./supply_hash_observation";
import { SupplyTransaction } from "./supply_transaction";
import { SupplyTssObservation } from "./supply_tss_observation";

export const protobufPackage = "cfprotocol.lock";

/** GenesisState defines the lock module's genesis state. */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  assetLockTransactionList: AssetLockTransaction[];
  assetLockTransactionCount: number;
  observeVoteLockingList: ObserveVoteLocking[];
  observeVoteLockingCount: number;
  lockBalanceList: LockBalance[];
  lockBalanceCount: number;
  supplyTransactionList: SupplyTransaction[];
  supplyTransactionCount: number;
  supplyTssObservationList: SupplyTssObservation[];
  supplyTssObservationCount: number;
  supplyHashObservationList: SupplyHashObservation[];
  supplyHashObservationCount: number;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    assetLockTransactionList: [],
    assetLockTransactionCount: 0,
    observeVoteLockingList: [],
    observeVoteLockingCount: 0,
    lockBalanceList: [],
    lockBalanceCount: 0,
    supplyTransactionList: [],
    supplyTransactionCount: 0,
    supplyTssObservationList: [],
    supplyTssObservationCount: 0,
    supplyHashObservationList: [],
    supplyHashObservationCount: 0,
  };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.assetLockTransactionList) {
      AssetLockTransaction.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.assetLockTransactionCount !== 0) {
      writer.uint32(24).uint64(message.assetLockTransactionCount);
    }
    for (const v of message.observeVoteLockingList) {
      ObserveVoteLocking.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.observeVoteLockingCount !== 0) {
      writer.uint32(40).uint64(message.observeVoteLockingCount);
    }
    for (const v of message.lockBalanceList) {
      LockBalance.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.lockBalanceCount !== 0) {
      writer.uint32(56).uint64(message.lockBalanceCount);
    }
    for (const v of message.supplyTransactionList) {
      SupplyTransaction.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.supplyTransactionCount !== 0) {
      writer.uint32(72).uint64(message.supplyTransactionCount);
    }
    for (const v of message.supplyTssObservationList) {
      SupplyTssObservation.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.supplyTssObservationCount !== 0) {
      writer.uint32(88).uint64(message.supplyTssObservationCount);
    }
    for (const v of message.supplyHashObservationList) {
      SupplyHashObservation.encode(v!, writer.uint32(98).fork()).join();
    }
    if (message.supplyHashObservationCount !== 0) {
      writer.uint32(104).uint64(message.supplyHashObservationCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.assetLockTransactionList.push(AssetLockTransaction.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.assetLockTransactionCount = longToNumber(reader.uint64());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.observeVoteLockingList.push(ObserveVoteLocking.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.observeVoteLockingCount = longToNumber(reader.uint64());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.lockBalanceList.push(LockBalance.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.lockBalanceCount = longToNumber(reader.uint64());
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.supplyTransactionList.push(SupplyTransaction.decode(reader, reader.uint32()));
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.supplyTransactionCount = longToNumber(reader.uint64());
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.supplyTssObservationList.push(SupplyTssObservation.decode(reader, reader.uint32()));
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.supplyTssObservationCount = longToNumber(reader.uint64());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.supplyHashObservationList.push(SupplyHashObservation.decode(reader, reader.uint32()));
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.supplyHashObservationCount = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      assetLockTransactionList: globalThis.Array.isArray(object?.assetLockTransactionList)
        ? object.assetLockTransactionList.map((e: any) => AssetLockTransaction.fromJSON(e))
        : [],
      assetLockTransactionCount: isSet(object.assetLockTransactionCount)
        ? globalThis.Number(object.assetLockTransactionCount)
        : 0,
      observeVoteLockingList: globalThis.Array.isArray(object?.observeVoteLockingList)
        ? object.observeVoteLockingList.map((e: any) => ObserveVoteLocking.fromJSON(e))
        : [],
      observeVoteLockingCount: isSet(object.observeVoteLockingCount)
        ? globalThis.Number(object.observeVoteLockingCount)
        : 0,
      lockBalanceList: globalThis.Array.isArray(object?.lockBalanceList)
        ? object.lockBalanceList.map((e: any) => LockBalance.fromJSON(e))
        : [],
      lockBalanceCount: isSet(object.lockBalanceCount) ? globalThis.Number(object.lockBalanceCount) : 0,
      supplyTransactionList: globalThis.Array.isArray(object?.supplyTransactionList)
        ? object.supplyTransactionList.map((e: any) => SupplyTransaction.fromJSON(e))
        : [],
      supplyTransactionCount: isSet(object.supplyTransactionCount)
        ? globalThis.Number(object.supplyTransactionCount)
        : 0,
      supplyTssObservationList: globalThis.Array.isArray(object?.supplyTssObservationList)
        ? object.supplyTssObservationList.map((e: any) => SupplyTssObservation.fromJSON(e))
        : [],
      supplyTssObservationCount: isSet(object.supplyTssObservationCount)
        ? globalThis.Number(object.supplyTssObservationCount)
        : 0,
      supplyHashObservationList: globalThis.Array.isArray(object?.supplyHashObservationList)
        ? object.supplyHashObservationList.map((e: any) => SupplyHashObservation.fromJSON(e))
        : [],
      supplyHashObservationCount: isSet(object.supplyHashObservationCount)
        ? globalThis.Number(object.supplyHashObservationCount)
        : 0,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.assetLockTransactionList?.length) {
      obj.assetLockTransactionList = message.assetLockTransactionList.map((e) => AssetLockTransaction.toJSON(e));
    }
    if (message.assetLockTransactionCount !== 0) {
      obj.assetLockTransactionCount = Math.round(message.assetLockTransactionCount);
    }
    if (message.observeVoteLockingList?.length) {
      obj.observeVoteLockingList = message.observeVoteLockingList.map((e) => ObserveVoteLocking.toJSON(e));
    }
    if (message.observeVoteLockingCount !== 0) {
      obj.observeVoteLockingCount = Math.round(message.observeVoteLockingCount);
    }
    if (message.lockBalanceList?.length) {
      obj.lockBalanceList = message.lockBalanceList.map((e) => LockBalance.toJSON(e));
    }
    if (message.lockBalanceCount !== 0) {
      obj.lockBalanceCount = Math.round(message.lockBalanceCount);
    }
    if (message.supplyTransactionList?.length) {
      obj.supplyTransactionList = message.supplyTransactionList.map((e) => SupplyTransaction.toJSON(e));
    }
    if (message.supplyTransactionCount !== 0) {
      obj.supplyTransactionCount = Math.round(message.supplyTransactionCount);
    }
    if (message.supplyTssObservationList?.length) {
      obj.supplyTssObservationList = message.supplyTssObservationList.map((e) => SupplyTssObservation.toJSON(e));
    }
    if (message.supplyTssObservationCount !== 0) {
      obj.supplyTssObservationCount = Math.round(message.supplyTssObservationCount);
    }
    if (message.supplyHashObservationList?.length) {
      obj.supplyHashObservationList = message.supplyHashObservationList.map((e) => SupplyHashObservation.toJSON(e));
    }
    if (message.supplyHashObservationCount !== 0) {
      obj.supplyHashObservationCount = Math.round(message.supplyHashObservationCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.assetLockTransactionList =
      object.assetLockTransactionList?.map((e) => AssetLockTransaction.fromPartial(e)) || [];
    message.assetLockTransactionCount = object.assetLockTransactionCount ?? 0;
    message.observeVoteLockingList = object.observeVoteLockingList?.map((e) => ObserveVoteLocking.fromPartial(e)) || [];
    message.observeVoteLockingCount = object.observeVoteLockingCount ?? 0;
    message.lockBalanceList = object.lockBalanceList?.map((e) => LockBalance.fromPartial(e)) || [];
    message.lockBalanceCount = object.lockBalanceCount ?? 0;
    message.supplyTransactionList = object.supplyTransactionList?.map((e) => SupplyTransaction.fromPartial(e)) || [];
    message.supplyTransactionCount = object.supplyTransactionCount ?? 0;
    message.supplyTssObservationList =
      object.supplyTssObservationList?.map((e) => SupplyTssObservation.fromPartial(e)) || [];
    message.supplyTssObservationCount = object.supplyTssObservationCount ?? 0;
    message.supplyHashObservationList =
      object.supplyHashObservationList?.map((e) => SupplyHashObservation.fromPartial(e)) || [];
    message.supplyHashObservationCount = object.supplyHashObservationCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
