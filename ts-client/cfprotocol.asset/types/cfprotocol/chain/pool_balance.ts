// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/chain/pool_balance.proto

/* eslint-disable */
// @ts-nocheck
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.chain";

/** Balance defines the balance structure. */
export interface Balance {
  assetId: number;
  symbol: string;
  name: string;
  amount: string;
  decimals: number;
  supplyApy: string;
  borrowApy: string;
  health: string;
}

/** PoolBalance defines the pool balance structure. */
export interface PoolBalance {
  index: string;
  chainName: string;
  balance: Balance[];
  nativeGasAmount: Balance | undefined;
}

function createBaseBalance(): Balance {
  return { assetId: 0, symbol: "", name: "", amount: "", decimals: 0, supplyApy: "", borrowApy: "", health: "" };
}

export const Balance: MessageFns<Balance> = {
  encode(message: Balance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assetId !== 0) {
      writer.uint32(8).uint64(message.assetId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.amount !== "") {
      writer.uint32(34).string(message.amount);
    }
    if (message.decimals !== 0) {
      writer.uint32(40).uint64(message.decimals);
    }
    if (message.supplyApy !== "") {
      writer.uint32(50).string(message.supplyApy);
    }
    if (message.borrowApy !== "") {
      writer.uint32(58).string(message.borrowApy);
    }
    if (message.health !== "") {
      writer.uint32(66).string(message.health);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Balance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.assetId = longToNumber(reader.uint64());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.amount = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.decimals = longToNumber(reader.uint64());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.supplyApy = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.borrowApy = reader.string();
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.health = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Balance {
    return {
      assetId: isSet(object.assetId) ? globalThis.Number(object.assetId) : 0,
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "",
      decimals: isSet(object.decimals) ? globalThis.Number(object.decimals) : 0,
      supplyApy: isSet(object.supplyApy) ? globalThis.String(object.supplyApy) : "",
      borrowApy: isSet(object.borrowApy) ? globalThis.String(object.borrowApy) : "",
      health: isSet(object.health) ? globalThis.String(object.health) : "",
    };
  },

  toJSON(message: Balance): unknown {
    const obj: any = {};
    if (message.assetId !== 0) {
      obj.assetId = Math.round(message.assetId);
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.amount !== "") {
      obj.amount = message.amount;
    }
    if (message.decimals !== 0) {
      obj.decimals = Math.round(message.decimals);
    }
    if (message.supplyApy !== "") {
      obj.supplyApy = message.supplyApy;
    }
    if (message.borrowApy !== "") {
      obj.borrowApy = message.borrowApy;
    }
    if (message.health !== "") {
      obj.health = message.health;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Balance>, I>>(base?: I): Balance {
    return Balance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Balance>, I>>(object: I): Balance {
    const message = createBaseBalance();
    message.assetId = object.assetId ?? 0;
    message.symbol = object.symbol ?? "";
    message.name = object.name ?? "";
    message.amount = object.amount ?? "";
    message.decimals = object.decimals ?? 0;
    message.supplyApy = object.supplyApy ?? "";
    message.borrowApy = object.borrowApy ?? "";
    message.health = object.health ?? "";
    return message;
  },
};

function createBasePoolBalance(): PoolBalance {
  return { index: "", chainName: "", balance: [], nativeGasAmount: undefined };
}

export const PoolBalance: MessageFns<PoolBalance> = {
  encode(message: PoolBalance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== "") {
      writer.uint32(10).string(message.index);
    }
    if (message.chainName !== "") {
      writer.uint32(18).string(message.chainName);
    }
    for (const v of message.balance) {
      Balance.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.nativeGasAmount !== undefined) {
      Balance.encode(message.nativeGasAmount, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PoolBalance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePoolBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.index = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.chainName = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.balance.push(Balance.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.nativeGasAmount = Balance.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PoolBalance {
    return {
      index: isSet(object.index) ? globalThis.String(object.index) : "",
      chainName: isSet(object.chainName) ? globalThis.String(object.chainName) : "",
      balance: globalThis.Array.isArray(object?.balance) ? object.balance.map((e: any) => Balance.fromJSON(e)) : [],
      nativeGasAmount: isSet(object.nativeGasAmount) ? Balance.fromJSON(object.nativeGasAmount) : undefined,
    };
  },

  toJSON(message: PoolBalance): unknown {
    const obj: any = {};
    if (message.index !== "") {
      obj.index = message.index;
    }
    if (message.chainName !== "") {
      obj.chainName = message.chainName;
    }
    if (message.balance?.length) {
      obj.balance = message.balance.map((e) => Balance.toJSON(e));
    }
    if (message.nativeGasAmount !== undefined) {
      obj.nativeGasAmount = Balance.toJSON(message.nativeGasAmount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PoolBalance>, I>>(base?: I): PoolBalance {
    return PoolBalance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PoolBalance>, I>>(object: I): PoolBalance {
    const message = createBasePoolBalance();
    message.index = object.index ?? "";
    message.chainName = object.chainName ?? "";
    message.balance = object.balance?.map((e) => Balance.fromPartial(e)) || [];
    message.nativeGasAmount = (object.nativeGasAmount !== undefined && object.nativeGasAmount !== null)
      ? Balance.fromPartial(object.nativeGasAmount)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
