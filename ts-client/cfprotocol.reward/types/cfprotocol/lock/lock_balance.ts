// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/lock/lock_balance.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.lock";

/** Balance defines balances */
export interface Balance {
  assetId: number;
  balance: string;
  /** this will be a consolidated interest rate. */
  interestRate: string;
}

/** LockBalance defines the user's lock balances */
export interface LockBalance {
  id: number;
  creator: string;
  ownerWallet: string;
  balances: Balance[];
}

function createBaseBalance(): Balance {
  return { assetId: 0, balance: "", interestRate: "" };
}

export const Balance: MessageFns<Balance> = {
  encode(message: Balance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assetId !== 0) {
      writer.uint32(8).uint64(message.assetId);
    }
    if (message.balance !== "") {
      writer.uint32(18).string(message.balance);
    }
    if (message.interestRate !== "") {
      writer.uint32(26).string(message.interestRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Balance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.assetId = longToNumber(reader.uint64());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.balance = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.interestRate = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Balance {
    return {
      assetId: isSet(object.assetId) ? globalThis.Number(object.assetId) : 0,
      balance: isSet(object.balance) ? globalThis.String(object.balance) : "",
      interestRate: isSet(object.interestRate) ? globalThis.String(object.interestRate) : "",
    };
  },

  toJSON(message: Balance): unknown {
    const obj: any = {};
    if (message.assetId !== 0) {
      obj.assetId = Math.round(message.assetId);
    }
    if (message.balance !== "") {
      obj.balance = message.balance;
    }
    if (message.interestRate !== "") {
      obj.interestRate = message.interestRate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Balance>, I>>(base?: I): Balance {
    return Balance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Balance>, I>>(object: I): Balance {
    const message = createBaseBalance();
    message.assetId = object.assetId ?? 0;
    message.balance = object.balance ?? "";
    message.interestRate = object.interestRate ?? "";
    return message;
  },
};

function createBaseLockBalance(): LockBalance {
  return { id: 0, creator: "", ownerWallet: "", balances: [] };
}

export const LockBalance: MessageFns<LockBalance> = {
  encode(message: LockBalance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.creator !== "") {
      writer.uint32(18).string(message.creator);
    }
    if (message.ownerWallet !== "") {
      writer.uint32(26).string(message.ownerWallet);
    }
    for (const v of message.balances) {
      Balance.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LockBalance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLockBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.creator = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.ownerWallet = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.balances.push(Balance.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LockBalance {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      ownerWallet: isSet(object.ownerWallet) ? globalThis.String(object.ownerWallet) : "",
      balances: globalThis.Array.isArray(object?.balances) ? object.balances.map((e: any) => Balance.fromJSON(e)) : [],
    };
  },

  toJSON(message: LockBalance): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.ownerWallet !== "") {
      obj.ownerWallet = message.ownerWallet;
    }
    if (message.balances?.length) {
      obj.balances = message.balances.map((e) => Balance.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LockBalance>, I>>(base?: I): LockBalance {
    return LockBalance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LockBalance>, I>>(object: I): LockBalance {
    const message = createBaseLockBalance();
    message.id = object.id ?? 0;
    message.creator = object.creator ?? "";
    message.ownerWallet = object.ownerWallet ?? "";
    message.balances = object.balances?.map((e) => Balance.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
