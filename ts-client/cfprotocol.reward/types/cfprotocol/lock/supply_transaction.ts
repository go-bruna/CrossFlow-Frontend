// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/lock/supply_transaction.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.lock";

/** SupplyTransaction defines the supply transaction */
export interface SupplyTransaction {
  id: number;
  lockId: number;
  status: string;
  tssHash: string;
  confirmedBlockHash: string;
  failReason: string;
  pullFailCount: number;
  processing: boolean;
  tssMsgId: string;
  timestamp: string;
  handleId: number;
  interestRate: string;
  reserved: string;
}

function createBaseSupplyTransaction(): SupplyTransaction {
  return {
    id: 0,
    lockId: 0,
    status: "",
    tssHash: "",
    confirmedBlockHash: "",
    failReason: "",
    pullFailCount: 0,
    processing: false,
    tssMsgId: "",
    timestamp: "",
    handleId: 0,
    interestRate: "",
    reserved: "",
  };
}

export const SupplyTransaction: MessageFns<SupplyTransaction> = {
  encode(message: SupplyTransaction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint64(message.id);
    }
    if (message.lockId !== 0) {
      writer.uint32(16).uint64(message.lockId);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.tssHash !== "") {
      writer.uint32(34).string(message.tssHash);
    }
    if (message.confirmedBlockHash !== "") {
      writer.uint32(42).string(message.confirmedBlockHash);
    }
    if (message.failReason !== "") {
      writer.uint32(50).string(message.failReason);
    }
    if (message.pullFailCount !== 0) {
      writer.uint32(56).uint64(message.pullFailCount);
    }
    if (message.processing !== false) {
      writer.uint32(64).bool(message.processing);
    }
    if (message.tssMsgId !== "") {
      writer.uint32(74).string(message.tssMsgId);
    }
    if (message.timestamp !== "") {
      writer.uint32(82).string(message.timestamp);
    }
    if (message.handleId !== 0) {
      writer.uint32(88).uint64(message.handleId);
    }
    if (message.interestRate !== "") {
      writer.uint32(98).string(message.interestRate);
    }
    if (message.reserved !== "") {
      writer.uint32(106).string(message.reserved);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SupplyTransaction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSupplyTransaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.uint64());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.lockId = longToNumber(reader.uint64());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.tssHash = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.confirmedBlockHash = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.failReason = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.pullFailCount = longToNumber(reader.uint64());
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.processing = reader.bool();
          continue;
        case 9:
          if (tag !== 74) {
            break;
          }

          message.tssMsgId = reader.string();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.handleId = longToNumber(reader.uint64());
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.interestRate = reader.string();
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.reserved = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SupplyTransaction {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      lockId: isSet(object.lockId) ? globalThis.Number(object.lockId) : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      tssHash: isSet(object.tssHash) ? globalThis.String(object.tssHash) : "",
      confirmedBlockHash: isSet(object.confirmedBlockHash) ? globalThis.String(object.confirmedBlockHash) : "",
      failReason: isSet(object.failReason) ? globalThis.String(object.failReason) : "",
      pullFailCount: isSet(object.pullFailCount) ? globalThis.Number(object.pullFailCount) : 0,
      processing: isSet(object.processing) ? globalThis.Boolean(object.processing) : false,
      tssMsgId: isSet(object.tssMsgId) ? globalThis.String(object.tssMsgId) : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
      handleId: isSet(object.handleId) ? globalThis.Number(object.handleId) : 0,
      interestRate: isSet(object.interestRate) ? globalThis.String(object.interestRate) : "",
      reserved: isSet(object.reserved) ? globalThis.String(object.reserved) : "",
    };
  },

  toJSON(message: SupplyTransaction): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.lockId !== 0) {
      obj.lockId = Math.round(message.lockId);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.tssHash !== "") {
      obj.tssHash = message.tssHash;
    }
    if (message.confirmedBlockHash !== "") {
      obj.confirmedBlockHash = message.confirmedBlockHash;
    }
    if (message.failReason !== "") {
      obj.failReason = message.failReason;
    }
    if (message.pullFailCount !== 0) {
      obj.pullFailCount = Math.round(message.pullFailCount);
    }
    if (message.processing !== false) {
      obj.processing = message.processing;
    }
    if (message.tssMsgId !== "") {
      obj.tssMsgId = message.tssMsgId;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.handleId !== 0) {
      obj.handleId = Math.round(message.handleId);
    }
    if (message.interestRate !== "") {
      obj.interestRate = message.interestRate;
    }
    if (message.reserved !== "") {
      obj.reserved = message.reserved;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SupplyTransaction>, I>>(base?: I): SupplyTransaction {
    return SupplyTransaction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SupplyTransaction>, I>>(object: I): SupplyTransaction {
    const message = createBaseSupplyTransaction();
    message.id = object.id ?? 0;
    message.lockId = object.lockId ?? 0;
    message.status = object.status ?? "";
    message.tssHash = object.tssHash ?? "";
    message.confirmedBlockHash = object.confirmedBlockHash ?? "";
    message.failReason = object.failReason ?? "";
    message.pullFailCount = object.pullFailCount ?? 0;
    message.processing = object.processing ?? false;
    message.tssMsgId = object.tssMsgId ?? "";
    message.timestamp = object.timestamp ?? "";
    message.handleId = object.handleId ?? 0;
    message.interestRate = object.interestRate ?? "";
    message.reserved = object.reserved ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
