// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/tss/type_keygen.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.tss";

/** KeygenType is enum for keygen type */
export enum KeygenType {
  PoolKeygen = 0,
  PoolKeyresharing = 1,
  PoolKeyresharingReady = 2,
  UNRECOGNIZED = -1,
}

export function keygenTypeFromJSON(object: any): KeygenType {
  switch (object) {
    case 0:
    case "PoolKeygen":
      return KeygenType.PoolKeygen;
    case 1:
    case "PoolKeyresharing":
      return KeygenType.PoolKeyresharing;
    case 2:
    case "PoolKeyresharingReady":
      return KeygenType.PoolKeyresharingReady;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KeygenType.UNRECOGNIZED;
  }
}

export function keygenTypeToJSON(object: KeygenType): string {
  switch (object) {
    case KeygenType.PoolKeygen:
      return "PoolKeygen";
    case KeygenType.PoolKeyresharing:
      return "PoolKeyresharing";
    case KeygenType.PoolKeyresharingReady:
      return "PoolKeyresharingReady";
    case KeygenType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Keygen is a data structure for key generation */
export interface Keygen {
  id: string;
  type: KeygenType;
  members: string[];
  newMembers: string[];
}

/** KeygenBlock is a data structure for key generation block */
export interface KeygenBlock {
  height: number;
  processed: boolean;
  keygens: Keygen[];
}

function createBaseKeygen(): Keygen {
  return { id: "", type: 0, members: [], newMembers: [] };
}

export const Keygen: MessageFns<Keygen> = {
  encode(message: Keygen, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    for (const v of message.members) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.newMembers) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Keygen {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeygen();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.members.push(reader.string());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.newMembers.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Keygen {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? keygenTypeFromJSON(object.type) : 0,
      members: globalThis.Array.isArray(object?.members) ? object.members.map((e: any) => globalThis.String(e)) : [],
      newMembers: globalThis.Array.isArray(object?.newMembers)
        ? object.newMembers.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Keygen): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== 0) {
      obj.type = keygenTypeToJSON(message.type);
    }
    if (message.members?.length) {
      obj.members = message.members;
    }
    if (message.newMembers?.length) {
      obj.newMembers = message.newMembers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Keygen>, I>>(base?: I): Keygen {
    return Keygen.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Keygen>, I>>(object: I): Keygen {
    const message = createBaseKeygen();
    message.id = object.id ?? "";
    message.type = object.type ?? 0;
    message.members = object.members?.map((e) => e) || [];
    message.newMembers = object.newMembers?.map((e) => e) || [];
    return message;
  },
};

function createBaseKeygenBlock(): KeygenBlock {
  return { height: 0, processed: false, keygens: [] };
}

export const KeygenBlock: MessageFns<KeygenBlock> = {
  encode(message: KeygenBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.height !== 0) {
      writer.uint32(8).int64(message.height);
    }
    if (message.processed !== false) {
      writer.uint32(16).bool(message.processed);
    }
    for (const v of message.keygens) {
      Keygen.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeygenBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeygenBlock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.height = longToNumber(reader.int64());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.processed = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.keygens.push(Keygen.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeygenBlock {
    return {
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      processed: isSet(object.processed) ? globalThis.Boolean(object.processed) : false,
      keygens: globalThis.Array.isArray(object?.keygens) ? object.keygens.map((e: any) => Keygen.fromJSON(e)) : [],
    };
  },

  toJSON(message: KeygenBlock): unknown {
    const obj: any = {};
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.processed !== false) {
      obj.processed = message.processed;
    }
    if (message.keygens?.length) {
      obj.keygens = message.keygens.map((e) => Keygen.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeygenBlock>, I>>(base?: I): KeygenBlock {
    return KeygenBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeygenBlock>, I>>(object: I): KeygenBlock {
    const message = createBaseKeygenBlock();
    message.height = object.height ?? 0;
    message.processed = object.processed ?? false;
    message.keygens = object.keygens?.map((e) => Keygen.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
