// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/cfnconsensus/consensus_payload.proto

/* eslint-disable */
// @ts-nocheck
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "cfprotocol.cfnconsensus";

/** Payload defines the necessary items for running a consensus algorithm */
export interface Payload {
  /** ob address that submitted the vote */
  voter: string;
  /** success or fail */
  result: boolean;
  /** value to be evaluated */
  value: string;
  /** if fails, attach error reason */
  errReason: string;
}

/** ConsensusPayload defines the payload for the consensus algorithm. */
export interface ConsensusPayload {
  payloads: Payload[];
}

function createBasePayload(): Payload {
  return { voter: "", result: false, value: "", errReason: "" };
}

export const Payload: MessageFns<Payload> = {
  encode(message: Payload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.voter !== "") {
      writer.uint32(10).string(message.voter);
    }
    if (message.result !== false) {
      writer.uint32(16).bool(message.result);
    }
    if (message.value !== "") {
      writer.uint32(26).string(message.value);
    }
    if (message.errReason !== "") {
      writer.uint32(34).string(message.errReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Payload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.voter = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.result = reader.bool();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.value = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.errReason = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Payload {
    return {
      voter: isSet(object.voter) ? globalThis.String(object.voter) : "",
      result: isSet(object.result) ? globalThis.Boolean(object.result) : false,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      errReason: isSet(object.errReason) ? globalThis.String(object.errReason) : "",
    };
  },

  toJSON(message: Payload): unknown {
    const obj: any = {};
    if (message.voter !== "") {
      obj.voter = message.voter;
    }
    if (message.result !== false) {
      obj.result = message.result;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.errReason !== "") {
      obj.errReason = message.errReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Payload>, I>>(base?: I): Payload {
    return Payload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Payload>, I>>(object: I): Payload {
    const message = createBasePayload();
    message.voter = object.voter ?? "";
    message.result = object.result ?? false;
    message.value = object.value ?? "";
    message.errReason = object.errReason ?? "";
    return message;
  },
};

function createBaseConsensusPayload(): ConsensusPayload {
  return { payloads: [] };
}

export const ConsensusPayload: MessageFns<ConsensusPayload> = {
  encode(message: ConsensusPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.payloads) {
      Payload.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConsensusPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConsensusPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.payloads.push(Payload.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConsensusPayload {
    return {
      payloads: globalThis.Array.isArray(object?.payloads) ? object.payloads.map((e: any) => Payload.fromJSON(e)) : [],
    };
  },

  toJSON(message: ConsensusPayload): unknown {
    const obj: any = {};
    if (message.payloads?.length) {
      obj.payloads = message.payloads.map((e) => Payload.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConsensusPayload>, I>>(base?: I): ConsensusPayload {
    return ConsensusPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConsensusPayload>, I>>(object: I): ConsensusPayload {
    const message = createBaseConsensusPayload();
    message.payloads = object.payloads?.map((e) => Payload.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
