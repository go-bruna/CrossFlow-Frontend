// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/tss/genesis.proto

/* eslint-disable */
// @ts-nocheck
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import { Pubkey } from "./pubkey";
import { TssPubkey } from "./tss_pubkey";

export const protobufPackage = "cfprotocol.tss";

/** GenesisState defines the tss module's genesis state. */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  pubkeyList: Pubkey[];
  pubkeyCount: number;
  tssPubkeyList: TssPubkey[];
  tssPubkeyCount: number;
}

function createBaseGenesisState(): GenesisState {
  return { params: undefined, pubkeyList: [], pubkeyCount: 0, tssPubkeyList: [], tssPubkeyCount: 0 };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.pubkeyList) {
      Pubkey.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.pubkeyCount !== 0) {
      writer.uint32(24).uint64(message.pubkeyCount);
    }
    for (const v of message.tssPubkeyList) {
      TssPubkey.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.tssPubkeyCount !== 0) {
      writer.uint32(40).uint64(message.tssPubkeyCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pubkeyList.push(Pubkey.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pubkeyCount = longToNumber(reader.uint64());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.tssPubkeyList.push(TssPubkey.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.tssPubkeyCount = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      pubkeyList: globalThis.Array.isArray(object?.pubkeyList)
        ? object.pubkeyList.map((e: any) => Pubkey.fromJSON(e))
        : [],
      pubkeyCount: isSet(object.pubkeyCount) ? globalThis.Number(object.pubkeyCount) : 0,
      tssPubkeyList: globalThis.Array.isArray(object?.tssPubkeyList)
        ? object.tssPubkeyList.map((e: any) => TssPubkey.fromJSON(e))
        : [],
      tssPubkeyCount: isSet(object.tssPubkeyCount) ? globalThis.Number(object.tssPubkeyCount) : 0,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.pubkeyList?.length) {
      obj.pubkeyList = message.pubkeyList.map((e) => Pubkey.toJSON(e));
    }
    if (message.pubkeyCount !== 0) {
      obj.pubkeyCount = Math.round(message.pubkeyCount);
    }
    if (message.tssPubkeyList?.length) {
      obj.tssPubkeyList = message.tssPubkeyList.map((e) => TssPubkey.toJSON(e));
    }
    if (message.tssPubkeyCount !== 0) {
      obj.tssPubkeyCount = Math.round(message.tssPubkeyCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.pubkeyList = object.pubkeyList?.map((e) => Pubkey.fromPartial(e)) || [];
    message.pubkeyCount = object.pubkeyCount ?? 0;
    message.tssPubkeyList = object.tssPubkeyList?.map((e) => TssPubkey.fromPartial(e)) || [];
    message.tssPubkeyCount = object.tssPubkeyCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
