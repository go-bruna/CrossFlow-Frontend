// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: cfprotocol/chain/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BalanceObservation } from "./balance_observation";
import { Chain } from "./chain";
import { Params } from "./params";
import { PoolBalance } from "./pool_balance";

export const protobufPackage = "cfprotocol.chain";

/** GenesisState defines the chain module's genesis state. */
export interface GenesisState {
  /** params defines all the parameters of the module. */
  params: Params | undefined;
  chainList: Chain[];
  chainCount: number;
  poolBalanceList: PoolBalance[];
  poolBalanceCount: number;
  balanceObservationList: BalanceObservation[];
  balanceObservationCount: number;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    chainList: [],
    chainCount: 0,
    poolBalanceList: [],
    poolBalanceCount: 0,
    balanceObservationList: [],
    balanceObservationCount: 0,
  };
}

export const GenesisState: MessageFns<GenesisState> = {
  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.chainList) {
      Chain.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.chainCount !== 0) {
      writer.uint32(24).uint64(message.chainCount);
    }
    for (const v of message.poolBalanceList) {
      PoolBalance.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.poolBalanceCount !== 0) {
      writer.uint32(40).uint64(message.poolBalanceCount);
    }
    for (const v of message.balanceObservationList) {
      BalanceObservation.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.balanceObservationCount !== 0) {
      writer.uint32(56).uint64(message.balanceObservationCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.chainList.push(Chain.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.chainCount = longToNumber(reader.uint64());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.poolBalanceList.push(PoolBalance.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.poolBalanceCount = longToNumber(reader.uint64());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.balanceObservationList.push(BalanceObservation.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.balanceObservationCount = longToNumber(reader.uint64());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      chainList: globalThis.Array.isArray(object?.chainList) ? object.chainList.map((e: any) => Chain.fromJSON(e)) : [],
      chainCount: isSet(object.chainCount) ? globalThis.Number(object.chainCount) : 0,
      poolBalanceList: globalThis.Array.isArray(object?.poolBalanceList)
        ? object.poolBalanceList.map((e: any) => PoolBalance.fromJSON(e))
        : [],
      poolBalanceCount: isSet(object.poolBalanceCount) ? globalThis.Number(object.poolBalanceCount) : 0,
      balanceObservationList: globalThis.Array.isArray(object?.balanceObservationList)
        ? object.balanceObservationList.map((e: any) => BalanceObservation.fromJSON(e))
        : [],
      balanceObservationCount: isSet(object.balanceObservationCount)
        ? globalThis.Number(object.balanceObservationCount)
        : 0,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.chainList?.length) {
      obj.chainList = message.chainList.map((e) => Chain.toJSON(e));
    }
    if (message.chainCount !== 0) {
      obj.chainCount = Math.round(message.chainCount);
    }
    if (message.poolBalanceList?.length) {
      obj.poolBalanceList = message.poolBalanceList.map((e) => PoolBalance.toJSON(e));
    }
    if (message.poolBalanceCount !== 0) {
      obj.poolBalanceCount = Math.round(message.poolBalanceCount);
    }
    if (message.balanceObservationList?.length) {
      obj.balanceObservationList = message.balanceObservationList.map((e) => BalanceObservation.toJSON(e));
    }
    if (message.balanceObservationCount !== 0) {
      obj.balanceObservationCount = Math.round(message.balanceObservationCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenesisState>, I>>(base?: I): GenesisState {
    return GenesisState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenesisState>, I>>(object: I): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.chainList = object.chainList?.map((e) => Chain.fromPartial(e)) || [];
    message.chainCount = object.chainCount ?? 0;
    message.poolBalanceList = object.poolBalanceList?.map((e) => PoolBalance.fromPartial(e)) || [];
    message.poolBalanceCount = object.poolBalanceCount ?? 0;
    message.balanceObservationList = object.balanceObservationList?.map((e) => BalanceObservation.fromPartial(e)) || [];
    message.balanceObservationCount = object.balanceObservationCount ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
